import React from 'react'

const Button = () => {
  return (
    <Button variant="contained" sx={{width: 300, height: 50, borderRadius: 3}}>
      <span>+</span>
      <span>Create New Group</span>
    </Button>
  )
}

export default Button
